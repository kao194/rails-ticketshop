<%= form_with(model: event, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :artist %>
    <%= form.text_field :artist, id: :event_artist %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :event_description %>
  </div>

  <div class="field">
    <%= form.label :price_low %>
    <%= form.number_field :price_low, id: :event_price_low, step: 0.01, min: 0 %>
  </div>

  <div class="field">
    <%= form.label :price_high %>
    <%= form.number_field :price_high, id: :event_price_high, step: 0.01, min: 0 %>
  </div>

  <div class="field">
    <%= form.label :event_date %>
    <%= form.date_field :event_date, id: :event_event_date%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
